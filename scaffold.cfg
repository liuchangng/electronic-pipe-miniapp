# scaffold.cfg - project start/stop configuration (key=value, edited by human)
# Used by start.bat / stop.bat generated by spec-superflow-enhanced.
# Leave a START value empty to skip that side. Ports are used by stop.bat to kill listeners.

CLIENT_START=npm run build:mp-weixin
SERVER_START=mvn spring-boot:run
ADMIN_START=npm run dev
CLIENT_PORT=5173
SERVER_PORT=8080
ADMIN_PORT=3001